kafka只能保证一个分区之内的消息有序性，不同分区之间是不可以的。由producer发送到一个特定的分区的消息将按它们被添加时的顺序来发送，
也就是说如果由相同的producer来发送两个消息m1、m2，若m1首先被发送，那么在日志中m1就比m2具有较低的offset。
consumer实例以日志中存储的顺序来查看消息。
对于复制因子n的topic，将能承受最多n-1服务器的故障，而不会丢失提交到日志的任何消息。